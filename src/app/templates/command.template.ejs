import * as utils from "../../common/utils";
import { Command } from "../../common/types";

export class <%= name %>Command implements Command {
  public command = "<%= optionGroup %>:<%= _.toLower(name) %>";
  public describe = "Description for the <%= name %> command.";

  public builder(yargs: any) {
    // TODO: add options for this command. Docs: http://yargs.js.org/docs/.
  }

  public handler = async (argv: any) => {
    // TODO: implement the things this command has to make. Docs: http://yargs.js.org/docs/.
  }
}